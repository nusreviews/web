import { RoundWholePipe } from './round-whole.pipe';

describe('RoundWholePipe', () => {
  it('create an instance', () => {
    const pipe = new RoundWholePipe();
    expect(pipe).toBeTruthy();
  });
});
